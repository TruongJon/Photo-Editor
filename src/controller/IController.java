package controller;

public interface IController {

  /**
   * Creates a new image processor for the user to interact with by calling upon the model.
   */
  void startImageProcessor();

}
