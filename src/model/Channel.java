package model;

/**
 * Enumeration to represent the different color channels within pixels of an image. Each enumeration
 * represents one of the 8-bit channels that create a 24-bit color sequence for a given pixel.
 */
public enum Channel {
  RED, GREEN, BLUE;
}
